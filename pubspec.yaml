name: graphql_query_builder
description: Creates a graphql query string from maps to use with http.
version: 1.0.0
repository: https://github.com/godfreycomputing/graphql_query_builder.git

environment:
  sdk: ">=2.12.0 <3.0.0"

#dependencies:
#  path: ^1.7.0
dev_dependencies:
  pedantic: ^1.9.0
